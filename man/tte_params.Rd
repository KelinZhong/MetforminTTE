% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{tte_params}
\alias{tte_params}
\title{Analysis parameters}
\usage{
tte_params(
  id_col,
  exposure_col,
  ps_formula,
  time_zero_col,
  death_status_col = NULL,
  death_date_col = NULL,
  match_calipers = numeric(0),
  ps_caliper = 0.05,
  year_days = 365,
  min_events = 0,
  followup_end = NULL,
  prev_affected_level = NULL,
  recode = identity,
  reader = NULL
)
}
\arguments{
\item{id_col}{Participant identifier column.}

\item{exposure_col}{Binary exposure column (1 = new user, 0 = eligible
control).}

\item{ps_formula}{Propensity-score formula; its left-hand side must be
\code{exposure_col}.}

\item{time_zero_col}{Date column supplying time zero. Both members of a
matched pair take the \strong{exposed} member's value.}

\item{death_status_col, death_date_col}{Mortality indicator and date.
Required for \code{model = "crr"} outcomes, where death is the competing
event.}

\item{match_calipers}{Named numeric vector of exact-scale calipers applied
jointly with the propensity caliper, e.g. \code{c(hba1c = 0.25)}. May be empty.}

\item{ps_caliper}{Propensity-score caliper. Default 0.05.}

\item{year_days}{Days per year. Default 365 — \strong{not} 365.25, matching the
original analysis.}

\item{min_events}{Minimum events before an outcome is fitted. Default 0,
which disables the guard. Raise it only after equivalence with a baseline
is established: a non-zero value suppresses fits the original produced.}

\item{followup_end}{End of administrative follow-up: a \code{Date}, a column
name, or \code{NULL} meaning each outcome's date column already carries the
event-or-censoring date for every row.}

\item{prev_affected_level}{Level of the \verb{*_prev} columns denoting presence
of the condition, e.g. \code{"1"} for 0/1 coding or \code{"2"} for 1/2 coding.
\code{NULL} (default) leaves the coding untouched. Read this off
\code{\link[=tte_validate]{tte_validate()}}'s tables — guessing it inverts every exclusion in the
study without producing a single error.}

\item{recode}{Function applied to the cohort after reading, or the string
\code{"ukb"} for this study's UK Biobank category collapsing. Default
\code{identity}.}

\item{reader}{Optional function overriding file-format dispatch, e.g.
\code{arrow::read_parquet}.}
}
\value{
An object of class \code{tte_params}.
}
\description{
Every column name and rule the pipeline needs. \code{id_col}, \code{exposure_col},
\code{ps_formula} and \code{time_zero_col} are required with no defaults, so a study
specific column name can never be inherited by accident.
}
\examples{
params <- tte_params(
  id_col = "eid", exposure_col = "met_user",
  ps_formula = met_user ~ age + sex + bmi,
  time_zero_col = "assessment_date",
  death_status_col = "death_status", death_date_col = "date_of_death",
  match_calipers = c(hba1c = 0.25),
  ps_caliper = 0.05)
params
}
