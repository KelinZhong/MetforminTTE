% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{tte_strategy}
\alias{tte_strategy}
\title{Analysis strategy}
\usage{
tte_strategy(
  type = c("itt", "pp", "custom"),
  rx = NULL,
  grace_days = 180,
  end_date = NULL,
  label = NULL,
  process = NULL,
  missing_rx = c("uncensored", "censor_at_zero", "error"),
  eid_col = "eid",
  date_col = "issue_date"
)
}
\arguments{
\item{type}{\code{"itt"}, \code{"pp"}, or \code{"custom"}. A function may be passed
directly as shorthand for a custom strategy.}

\item{rx}{Dispensing records (path or data.frame) for \code{"pp"}. Read eagerly,
so malformed dates fail here rather than mid-run.}

\item{grace_days}{Days added to the last dispensing before censoring.}

\item{end_date}{Optional cap (administrative end of dispensing data).}

\item{label}{Strategy label; used in filenames and plot legends. Defaults
to \code{"ITT"}, \code{"PP"} or \code{"custom"}.}

\item{process}{For \code{"custom"}, a function of \verb{(surv, matched, params)}.}

\item{missing_rx}{Behaviour for an exposed participant with no dispensing
records: \code{"uncensored"} (default, believed to reproduce the original),
\code{"censor_at_zero"}, or \code{"error"}. Confirm against your baseline.}

\item{eid_col, date_col}{Column names in the dispensing data.}
}
\value{
An object of class \code{tte_strategy}.
}
\description{
A strategy is the estimand plus the mechanism that produces it. Three
concepts are kept separate on purpose: the \emph{label} is what you claim
(intention-to-treat, per-protocol), the \emph{mechanism} is whatever rewrites
event times, and the \emph{data} driving it is an argument. Collapsing these is
how "per-protocol" comes to mean whichever censoring rule someone wrote
last.
}
\section{Processor contract}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{in:  surv - time zero set, outcomes NOT yet derived
out: surv - status / incidence / date columns rewritten as needed
     may add a `censor_date` column; follow-up is capped at it
     output MUST NOT contain any time_to_* or *_incid_cr column
}\if{html}{\out{</div>}}

The last line is enforced; derivation runs afterwards. A processor that
removes events must also write \code{censor_date}, or follow-up will not shorten
to match and the participant gains person-time they never contributed.
}

\section{Per-protocol assumptions}{

Controls inherit their matched exposed participant's censoring date, so
pair follow-up stays symmetric — but a control's censoring is driven by
someone else's prescribing history. Administrative censoring at
discontinuation without weighting identifies the per-protocol effect only
under non-informative discontinuation; if discontinuation is informative,
take \code{\link[=tte_analysis_data]{tte_analysis_data()}} and fit weighted models yourself.
}

\examples{
itt <- tte_strategy("itt")
itt

rx <- data.frame(eid = c("P001", "P001", "P002"),
                 issue_date = as.Date(c("2011-01-01", "2012-06-01",
                                        "2011-03-01")))
pp <- tte_strategy("pp", rx = rx, grace_days = 180)
pp

## a marker strategy proves the hook runs before derivation, not merely
## that it exists
marker <- tte_strategy(function(surv, matched, params) {
  surv$oa_date <- surv$time_zero + 365
  surv
})
marker$label
}
