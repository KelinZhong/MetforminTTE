% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{tte_outcomes}
\alias{tte_outcomes}
\title{Outcome registry}
\usage{
tte_outcomes(
  key,
  label,
  prev = NA_character_,
  status = NA_character_,
  incid = NA_character_,
  date = NA_character_,
  model = "crr",
  in_plot = TRUE,
  components = NULL,
  from = "cr",
  dates = NULL
)
}
\arguments{
\item{key}{Short identifier; names the derived columns \verb{time_to_<key>} and
\verb{<key>_incid_cr}, and merges arms in combined forest plots. Must be
unique.}

\item{label}{Display label.}

\item{prev}{Baseline-prevalence column used to exclude pairs. For a derived
outcome, \code{NA} means "union of the components' prevalence flags".}

\item{status}{Baseline-status column. Used only by censoring; outcome
derivation never sees it.}

\item{incid}{First-occurrence indicator column.}

\item{date}{Diagnosis-date column.}

\item{model}{\code{"crr"} (Fine-Gray, death competing) or \code{"cox"} (mortality
itself, which has no competing event).}

\item{in_plot}{Whether the outcome appears in forest plots. \code{FALSE} keeps it
in the results table but out of the plot, replacing positional row drops.}

\item{components}{Character vector of elementary keys. Supplying this makes
a derived outcome; \code{key} must then be length 1.}

\item{from}{\code{"cr"} or \code{"raw"}, length 1 (recycled) or \code{length(components)}.}

\item{dates}{Date columns for time-to-first-event in a derived outcome.
Defaults to the components' own date columns.}
}
\value{
A \code{tte_outcome_registry} (a \code{data.table}). Combine pieces with
\code{rbind()}.
}
\description{
One row per outcome. Column names are supplied per outcome, so irregular
naming is configuration rather than a code special case — an outcome whose
date column is \code{any_can_date} instead of \code{any_can_diag_date} needs no code
change.
}
\details{
Elementary outcomes are vectorised: pass vectors and scalars recycle.
Supplying \code{components} instead builds a single \strong{derived} (composite)
outcome from elementary keys.

\code{from} records, per component, whether the composite consumes that
component's competing-risk-recoded indicator (\code{"cr"}) or its raw incidence
indicator (\code{"raw"}). This exists so a known inconsistency in an original
analysis can be reproduced as recorded configuration and corrected later
with a one-argument edit rather than a code change.
}
\examples{
keys <- c("dementia", "stroke", "diabetes")
elementary <- tte_outcomes(
  key    = keys,
  label  = c("Dementia", "Stroke", "Type 2 diabetes"),
  prev   = paste0(keys, "_prev"),
  status = paste0(keys, "_status"),
  incid  = paste0(keys, "_incid"),
  date   = paste0(keys, "_diag_date"),
  in_plot = keys != "diabetes")

## irregular column names are absorbed here, not special-cased in code
cancer <- tte_outcomes("any_can", "Any cancer", "any_can_prev",
                       "any_can_status", "any_can_incid",
                       date = "any_can_date")

## mortality has no competing event, so it is a Cox model
mortality <- tte_outcomes("mortality", "All-cause mortality",
                          status = "death_status", incid = "death_status",
                          date = "date_of_death", model = "cox")

## a composite; prev defaults to the union of its components' flags
composite <- tte_outcomes("composite", "Composite age-related disease",
                          components = c("dementia", "stroke"), from = "cr")

outcomes <- rbind(elementary, cancer, mortality, composite)
outcomes
}
